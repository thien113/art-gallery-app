export default function FavoriteFilter ({artPiecesInfo, onToggleFavorite}){
    return (
        <h2>My Favorites</h2>
        // {artPiecesInfo.filter((artPiecesInfo) => artPiecesInfo.slug.isFavorite===true)(
        //     return 
        // )}
    )
}

